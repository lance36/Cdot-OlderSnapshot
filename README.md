# Cdot-OlderSnapshot
This simple script displays all the snapshot older than $days on a Cluster-Mode Netapp Storage System.
**Requires NetApp PowerShell Toolkit**

**Usage:**
.\olderSnapshot.ps1 –s "Cluster" -u “Username” –p “Password” –d “Days”